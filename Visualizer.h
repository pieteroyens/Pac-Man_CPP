#ifndef RENDER_H
#define RENDER_H

#include "Map.h"
#include "Level.h"

//void draw_level(sf::RenderWindow& window, Map& map);
void draw_level(sf::RenderWindow& window, Level& level);

#endif // !RENDER_H

